<nav style="width: 100%">
	<i class='bx bx-menu'></i>

	<a href="#" class="notification">
		<i class='bx bxs-bell'></i>
	</a>
	<a href="#" class="profile">
	</a>
	<!-- <div id="google_translate_element" style="float: right"></div>

	<script type="text/javascript">
		function googleTranslateElementInit() {
			new google.translate.TranslateElement({ pageLanguage: 'en' }, 'google_translate_element');
		}
	</script>

	<script type="text/javascript"
		src="//translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script>
	<script>
		function removeTranslateDiv() {
			// Find the Google Translate div
			var translateDiv = document.querySelector('.skiptranslate');

			// Check if the div exists
			if (translateDiv?.children?.length >= 2) {
				// Remove the second child
				translateDiv.removeChild(translateDiv.children[1]);
				translateDiv.childNodes.forEach(function (node) {
					// Check if the node is a text node and contains the "Powered by" text
					if (node.nodeType === Node.TEXT_NODE && node.textContent.includes('Powered by')) {
						// Remove the text node
						node.parentNode.removeChild(node);
						console.log('Powered by text removed from the div.');
					}
				});
			} else {
			}
		}

		// Call the function initially
		removeTranslateDiv();

		// Set an interval to continuously check for the div
		setInterval(removeTranslateDiv, 500);
	</script> -->
</nav>